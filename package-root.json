{
  "name": "gestion-q24",
  "version": "2.0.0",
  "description": "Sistema SaaS de Gestión para Minimercados",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && npm start --prefix backend",
    "setup": "npm install && npm run setup --prefix backend && npm run setup --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}